server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: auth-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/marketgrid_auth
